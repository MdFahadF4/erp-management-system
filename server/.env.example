# Copy to server/.env (never commit server/.env)

PORT=5000

# Paste your Atlas SRV connection string here (Database → Connect → Drivers → Node.js)
# Do not commit real credentials to Git.
MONGODB_URI=

# If SRV fails on Windows, use Atlas "Standard connection string" instead:
# MONGODB_URI_STANDARD=

CLIENT_TOKEN=MEHRIN-MASTER_TEMPLATE_TOKEN_17062026
ADMIN_ACCOUNT_STATUS=ACTIVE
CLIENT_ORIGIN=http://localhost:5173

# First login (auto-created when DB is empty): superadmin / password123
